{
  "name": "kudu",
  "version": "0.1.0",
  "scripts": {
    "compile": "6to5 -d lib/ src/**/*.es src/*.es test/*.es",
    "test": "npm run compile && mocha lib/test/ -R spec --recursive"
  },
  "main": "lib/src/kudu.js",
  "dependencies": {
    "jsonschema": "^1.0.0"
  },
  "devDependencies": {
    "6to5": "^3.5.0",
    "chai": "^1.10.0",
    "mocha": "^2.0.1",
    "express": "^4.0.0"
  }
}
