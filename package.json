{
  "name": "kudu",
  "version": "0.1.0",
  "scripts": {
    "compile": "babel -d lib/ src/**/*.es6 src/*.es6 test/**/*.es6 test/*.es6",
    "test": "npm run compile && mocha lib/test/ -R spec --recursive"
  },
  "main": "lib/src/kudu.js",
  "dependencies": {
    "jsonschema": "^1.0.0",
    "core-js": "^0.5.2",
    "lodash": "^3.2.0"
  },
  "devDependencies": {
    "babel": "^4.0.1",
    "chai": "^1.10.0",
    "mocha": "^2.0.1",
    "express": "^4.0.0",
    "supertest": "^0.15.0",
    "body-parser": "^1.10.0"
  }
}
